// AppMode: Enumarated type

const AppMode = {
  WEATHER: "WeatherMode",
  //LOGIN: "LoginMode",
  SITEMAP: "SiteMode",
};

Object.freeze(AppMode);  // Ensuring the object is immutable

export default AppMode;